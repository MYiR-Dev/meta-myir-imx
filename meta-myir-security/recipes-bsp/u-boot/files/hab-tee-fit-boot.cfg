# i.MX6ULL HAB+TEE FIT boot command
# invalid after first flash (all 0xFF in the env area).
#CONFIG_BOOTCOMMAND="if test ${bootslot} = singlerescue;then run swuboot; fi; run adjustbootsource;run findfdt;run findtee;mmc dev ${mmcdev}; if mmc rescan; then if run loadbootscript; then run bootscript; else if fatload mmc ${mmcdev}:${mmcpart} ${loadaddr} fitImage; then run select_bootslot; run mmcargs; bootm ${loadaddr}; else run netboot; fi; fi; else run netboot; fi"
CONFIG_BOOTCOMMAND="if test ${bootslot} = singlerescue;then run swuboot; fi; run adjustbootsource;run findfdt;run findtee;mmc dev ${mmcdev}; if mmc rescan; then if run loadbootscript; then run bootscript; else if run loadfit; then run mmchabfitboot; else run netboot; fi; fi; else run netboot; fi"
CONFIG_SYS_LOAD_ADDR=0x82000000
