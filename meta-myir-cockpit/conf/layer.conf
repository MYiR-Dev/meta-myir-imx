# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have a packages directory, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-myir-cockpit"
BBFILE_PATTERN_meta-myir-cockpit := "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-myir-cockpit = "8"
LAYERSERIES_COMPAT_meta-myir-cockpit = "langdale mickledore nanbield scarthgap styhead walnascar"
